{
  "name": "@osai/observability",
  "version": "0.0.1",
  "description": "osaI Observability -- pino Logger + Audit + OpenTelemetry (DOMAIN-010)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=22.16.0"
  },
  "dependencies": {
    "pino": "^10.3.1",
    "better-sqlite3": "^12.8.0"
  },
  "devDependencies": {
    "@osai/shared": "workspace:*",
    "@types/better-sqlite3": "^7.6.13",
    "pino-pretty": "^13.1.3"
  }
}
