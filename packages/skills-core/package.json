{
  "name": "@osai/skills-core",
  "version": "0.0.1",
  "description": "osaI Bundled Skills -- Filesystem, Shell, Browser, HTTP (DOMAIN-003)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=22.16.0"
  },
  "dependencies": {
    "pino": "^10.3.1",
    "@osai/shared": "workspace:*",
    "@osai/observability": "workspace:*"
  }
}
